[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["apps/api", "apps/worker", "packages/db", "packages/redis", "packages/shared"]

[tool.hatch.build.targets.sdist]
include = [
    "/apps",
    "/packages",
    "/configs",
]
